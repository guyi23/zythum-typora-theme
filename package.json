{
  "scripts": {
    "repo": "npm run git:add && npm run git:commit && npm run git:push",
    "git:add": "git add .",
    "git:commit": "git commit -m 'chore: commit'",
    "git:push": "git push origin master"
  },
  "dependencies": {
    "sass": "^1.77.6"
  }
}
